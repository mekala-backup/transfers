python3 -c "
import json

path = '/home/ubuntu/dreamzero/so101-table-cleanup/meta/episodes.jsonl'
episodes = [json.loads(l) for l in open(path)]
valid = [e for e in episodes if e['length'] >= 24]

# Re-index sequentially
for i, e in enumerate(valid):
    e['episode_index'] = i

print(f'Kept {len(valid)} episodes, re-indexed 0-{len(valid)-1}')

with open(path, 'w') as f:
    for e in valid:
        f.write(json.dumps(e) + '\n')
print('Done')
"
